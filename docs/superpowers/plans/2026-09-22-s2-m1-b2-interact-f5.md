# S2-M1-B2 互动触发件批 · F5 人工验收单（Windows）

批范围 headless 已跑：**23 跑 22 绿 + 1 红归因用户侧资产**（见末条，非本批代码引燃）。
本单只做机器测不了的事。

**前置纪律（本批必做）**：
- 同步到 `s2-m1-b2-done` 后**重启 Godot 编辑器**——本批外部改过 `project.godot`
  （新增 `interact` 动作=物理键 E）；长会话旧内存会在下次设置保存时把它回写抹掉（判例）。
- 重启后 Project Settings → Input Map 应看见 `interact` 绑 E。看不见=Syncthing 未到或回写事故，报我。

## 验收点

1. **容器皮肤肉眼关账（B1 遗留验收，本批补上场地）**：F6 跑
   `res://tools/container_contract/fixtures/chapter_fix.tscn`。三段现在各有色块地面
   （A 土黄 / B 青绿 / C 紫灰）+ 巨型 SEG 名牌 + x=620 一条**绿色触发线带**。
   过线刷怪→清怪→换段时，**地板颜色和名牌应一眼换代**；死亡（站着让鸟啄死）应回本段
   入口满血、颜色不变。换段 ~1.5 秒"停顿→清场→落位"节拍先给主观印象——
   **最终手感裁决按裁决留到 B6 实段**（节奏全是常数，届时翻案=改一行）。
   注：名牌在战斗推镜（zoom 2.29）时可能只露一角，拉远后应清晰。
2. **互动游乐场**：F6 跑 `res://tools/interact_contract/fixtures/chapter_it2.tscn`
   与 `chapter_it3.tscn`（真键盘 E）：
   - it2 宝箱段：走近出"E"提示 → 按 E 开箱（缩小消失，关键道具旗落账）→ 再按无效；
   - it2 船闸段：按 E 门开（上滑）→ 站着别动约 1 秒 → **整段强制翻页**到你未到达的下幕
     ——这就是"历史不可变"的机器演出；残血会被原样带进新段（强制推进不是免费治疗）；
   - it3 跳河段：窗口期（Prompt 闪烁节奏）按 E=成功入水推进；不按=每个窗口吃 15 伤、
     **最低打到 1 血永不死**、窗口继续再来——悲剧只延迟不否决。
   - QTE/门在暂停菜单开着时**仍会走表**（已知未裁项，B7 暂停整合批一并裁，不是 bug 报告对象）。
3. **E7/E8 子进程流在 Windows 全绿**：F6 跑
   `res://tools/container_contract/container_contract.tscn`，确认 E7a/b + E8a/b/c 全 PASS
   （E8b 的 resume 指纹判据设计给 Windows；Linux 已实证引擎静默解绑、由 E8c 结构锁承重）。
4. **input_channel 红灯协查（用户侧）**：本批期间 `tools/input_channel_test` 的
   "AI 注入链路"一项确定性红，同套物理键盘出拳绿。取证指向今日 18:15 你的编辑器会话
   对 `characters/playable/chen/**`（非 git 管理）的批量回写（23 处 unique_id 签名）。
   请回想今天是否在编辑器改过 chen（皮肤/动画/数值）？若"没动过"也回写=又一起长会话
   吞改事故，把 `chen_skin.tscn` 与 animations 目录截图/mtime 发我，我们走资产侧排查。
   **本批矩阵其余 22 跑全绿，唯此红挂在你侧，批关账不为其背书也不阻塞。**

任何一条异常：截图/日志发回，按 SDD 修复轮回炉。
